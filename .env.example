# Copy to .env and fill in before `docker compose up -d`.
POSTGRES_USER=pgmem
# Required. Pick your own strong password; compose refuses to start without it.
POSTGRES_PASSWORD=
POSTGRES_DB=pgmem
POSTGRES_PORT=5432
